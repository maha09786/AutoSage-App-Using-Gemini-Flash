<!DOCTYPE html>
<html>
<head>
  <title>AutoSage AI</title>
  <style>
    body { font-family: Arial; text-align: center; background:#f2f2f2; }
    h1 { color:#2c3e50; }
    input { width:60%; padding:10px; }
    button { padding:10px 20px; background:#2c3e50; color:white; border:none; }
    #chat { margin-top:20px; width:70%; margin-left:auto; margin-right:auto; }
    .msg { background:white; padding:10px; margin:10px; border-radius:8px; }
  </style>
</head>
<body>

<h1>🚗 AutoSage AI Assistant</h1>

<input id="question" placeholder="Describe your vehicle problem..." />
<button onclick="ask()">Ask</button>

<div id="chat"></div>

<script>
async function ask() {
  const question = document.getElementById("question").value;

  const res = await fetch("/ask", {
    method: "POST",
    headers: {"Content-Type": "application/json"},
    body: JSON.stringify({question})
  });

  const data = await res.json();

  document.getElementById("chat").innerHTML +=
    `<div class="msg"><b>You:</b> ${question}</div>` +
    `<div class="msg"><b>AutoSage:</b> ${data.reply}</div>`;
}
</script>

</body>
</html>
